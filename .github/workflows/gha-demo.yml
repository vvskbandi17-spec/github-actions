name: Hello world
on: 
  push:
    branches:
      - main
      - development
      - 'release/**'
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: echo "Running a build Step"

 
  
